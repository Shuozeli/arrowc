[package]
name = "arrowc-schema"
version = "0.1.0"
edition = "2021"
description = "Arrow schema IR and input parsing for arrowc"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"
thiserror = "2"
