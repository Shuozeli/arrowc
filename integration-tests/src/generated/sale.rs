// Generated by arrowc. Do not edit.
#![allow(clippy::too_many_arguments)]

use std::sync::Arc;

use arrow::array::{
    ArrayBuilder, ArrayRef, AsArray, Date32Array, Date32Builder, Decimal128Array,
    Decimal128Builder, Float64Builder, Int32Builder, Int64Array, Int64Builder, ListArray,
    ListBuilder, RecordBatch, StringArray, StringBuilder, StructArray, StructBuilder,
};
use arrow::datatypes::{DataType, Field, Fields, Schema};
use arrow::error::ArrowError;

/// Companion struct for the `SaleItems` nested type.
pub struct SaleItems {
    pub product_id: Option<i64>,
    pub quantity: Option<i32>,
    pub unit_price: Option<f64>,
}

/// Companion struct for the `SaleShippingAddress` nested type.
pub struct SaleShippingAddress<'a> {
    pub street: Option<&'a str>,
    pub city: Option<&'a str>,
    pub state: Option<&'a str>,
    pub zip: Option<&'a str>,
}

/// Schema definition for Sale.
pub struct SaleSchema;

impl SaleSchema {
    /// Returns the Arrow schema for Sale.
    pub fn schema() -> Schema {
        Schema::new(vec![
            Field::new("sale_id", DataType::Int64, false),
            Field::new("customer_name", DataType::Utf8, false),
            Field::new("sale_date", DataType::Date32, true),
            Field::new(
                "items",
                DataType::List(Arc::new(Field::new(
                    "item",
                    DataType::Struct(Fields::from(vec![
                        Field::new("product_id", DataType::Int64, true),
                        Field::new("quantity", DataType::Int32, true),
                        Field::new("unit_price", DataType::Float64, true),
                    ])),
                    true,
                ))),
                true,
            ),
            Field::new(
                "shipping_address",
                DataType::Struct(Fields::from(vec![
                    Field::new("street", DataType::Utf8, true),
                    Field::new("city", DataType::Utf8, true),
                    Field::new("state", DataType::Utf8, true),
                    Field::new("zip", DataType::Utf8, true),
                ])),
                true,
            ),
            Field::new("total_amount", DataType::Decimal128(12, 2), true),
            Field::new("notes", DataType::Utf8, true),
        ])
    }

    /// Returns the Arrow schema for Sale wrapped in an Arc.
    pub fn schema_ref() -> Arc<Schema> {
        Arc::new(Self::schema())
    }
}

/// Typed builder for Sale RecordBatches.
pub struct SaleBuilder {
    sale_id: Int64Builder,
    customer_name: StringBuilder,
    sale_date: Date32Builder,
    items: ListBuilder<StructBuilder>,
    shipping_address: StructBuilder,
    total_amount: Decimal128Builder,
    notes: StringBuilder,
}

impl SaleBuilder {
    /// Create a new builder with default capacity.
    pub fn new() -> Self {
        Self {
            sale_id: Int64Builder::new(),
            customer_name: StringBuilder::new(),
            sale_date: Date32Builder::new(),
            items: ListBuilder::new(StructBuilder::new(
                vec![
                    Field::new("product_id", DataType::Int64, true),
                    Field::new("quantity", DataType::Int32, true),
                    Field::new("unit_price", DataType::Float64, true),
                ],
                vec![
                    Box::new(Int64Builder::new()) as Box<dyn ArrayBuilder>,
                    Box::new(Int32Builder::new()) as Box<dyn ArrayBuilder>,
                    Box::new(Float64Builder::new()) as Box<dyn ArrayBuilder>,
                ],
            )),
            shipping_address: StructBuilder::new(
                vec![
                    Field::new("street", DataType::Utf8, true),
                    Field::new("city", DataType::Utf8, true),
                    Field::new("state", DataType::Utf8, true),
                    Field::new("zip", DataType::Utf8, true),
                ],
                vec![
                    Box::new(StringBuilder::new()) as Box<dyn ArrayBuilder>,
                    Box::new(StringBuilder::new()) as Box<dyn ArrayBuilder>,
                    Box::new(StringBuilder::new()) as Box<dyn ArrayBuilder>,
                    Box::new(StringBuilder::new()) as Box<dyn ArrayBuilder>,
                ],
            ),
            total_amount: Decimal128Builder::new()
                .with_precision_and_scale(12, 2)
                .unwrap(),
            notes: StringBuilder::new(),
        }
    }

    /// Create a new builder with the given capacity hint.
    pub fn with_capacity(capacity: usize) -> Self {
        Self {
            sale_id: Int64Builder::with_capacity(capacity),
            customer_name: StringBuilder::with_capacity(capacity, capacity * 32),
            sale_date: Date32Builder::with_capacity(capacity),
            items: ListBuilder::with_capacity(
                StructBuilder::new(
                    vec![
                        Field::new("product_id", DataType::Int64, true),
                        Field::new("quantity", DataType::Int32, true),
                        Field::new("unit_price", DataType::Float64, true),
                    ],
                    vec![
                        Box::new(Int64Builder::new()) as Box<dyn ArrayBuilder>,
                        Box::new(Int32Builder::new()) as Box<dyn ArrayBuilder>,
                        Box::new(Float64Builder::new()) as Box<dyn ArrayBuilder>,
                    ],
                ),
                capacity,
            ),
            shipping_address: StructBuilder::new(
                vec![
                    Field::new("street", DataType::Utf8, true),
                    Field::new("city", DataType::Utf8, true),
                    Field::new("state", DataType::Utf8, true),
                    Field::new("zip", DataType::Utf8, true),
                ],
                vec![
                    Box::new(StringBuilder::new()) as Box<dyn ArrayBuilder>,
                    Box::new(StringBuilder::new()) as Box<dyn ArrayBuilder>,
                    Box::new(StringBuilder::new()) as Box<dyn ArrayBuilder>,
                    Box::new(StringBuilder::new()) as Box<dyn ArrayBuilder>,
                ],
            ),
            total_amount: Decimal128Builder::with_capacity(capacity)
                .with_precision_and_scale(12, 2)
                .unwrap(),
            notes: StringBuilder::with_capacity(capacity, capacity * 32),
        }
    }

    /// Append a row to the builder.
    pub fn append(
        &mut self,
        sale_id: i64,
        customer_name: &str,
        sale_date: Option<i32>,
        items: Option<&[SaleItems]>,
        shipping_address: Option<&SaleShippingAddress<'_>>,
        total_amount: Option<i128>,
        notes: Option<&str>,
    ) {
        self.sale_id.append_value(sale_id);
        self.customer_name.append_value(customer_name);
        self.sale_date.append_option(sale_date);
        match items {
            Some(rows) => {
                for row in rows {
                    self.items
                        .values()
                        .field_builder::<Int64Builder>(0)
                        .unwrap()
                        .append_option(row.product_id);
                    self.items
                        .values()
                        .field_builder::<Int32Builder>(1)
                        .unwrap()
                        .append_option(row.quantity);
                    self.items
                        .values()
                        .field_builder::<Float64Builder>(2)
                        .unwrap()
                        .append_option(row.unit_price);
                    self.items.values().append(true);
                }
                self.items.append(true);
            }
            None => {
                self.items.append(false);
            }
        }
        match shipping_address {
            Some(s) => {
                self.shipping_address
                    .field_builder::<StringBuilder>(0)
                    .unwrap()
                    .append_option(s.street);
                self.shipping_address
                    .field_builder::<StringBuilder>(1)
                    .unwrap()
                    .append_option(s.city);
                self.shipping_address
                    .field_builder::<StringBuilder>(2)
                    .unwrap()
                    .append_option(s.state);
                self.shipping_address
                    .field_builder::<StringBuilder>(3)
                    .unwrap()
                    .append_option(s.zip);
                self.shipping_address.append(true);
            }
            None => {
                self.shipping_address
                    .field_builder::<StringBuilder>(0)
                    .unwrap()
                    .append_null();
                self.shipping_address
                    .field_builder::<StringBuilder>(1)
                    .unwrap()
                    .append_null();
                self.shipping_address
                    .field_builder::<StringBuilder>(2)
                    .unwrap()
                    .append_null();
                self.shipping_address
                    .field_builder::<StringBuilder>(3)
                    .unwrap()
                    .append_null();
                self.shipping_address.append(false);
            }
        }
        self.total_amount.append_option(total_amount);
        match notes {
            Some(v) => self.notes.append_value(v),
            None => self.notes.append_null(),
        }
    }

    /// Finish building and return the RecordBatch.
    pub fn finish(&mut self) -> Result<RecordBatch, ArrowError> {
        let columns: Vec<ArrayRef> = vec![
            Arc::new(self.sale_id.finish()),
            Arc::new(self.customer_name.finish()),
            Arc::new(self.sale_date.finish()),
            Arc::new(self.items.finish()),
            Arc::new(self.shipping_address.finish()),
            Arc::new(self.total_amount.finish()),
            Arc::new(self.notes.finish()),
        ];
        RecordBatch::try_new(SaleSchema::schema_ref(), columns)
    }

    /// Return the number of rows appended so far.
    pub fn len(&self) -> usize {
        self.sale_id.len()
    }

    /// Returns true if no rows have been appended.
    pub fn is_empty(&self) -> bool {
        self.len() == 0
    }
}

impl Default for SaleBuilder {
    fn default() -> Self {
        Self::new()
    }
}

/// Typed reader for Sale RecordBatches.
///
/// Provides typed column accessors that return the correct Arrow array
/// types without requiring manual downcasting.
pub struct SaleReader<'a> {
    batch: &'a RecordBatch,
}

impl<'a> SaleReader<'a> {
    /// Create a reader for the given RecordBatch.
    ///
    /// Returns an error if the batch schema does not match Sale.
    pub fn try_new(batch: &'a RecordBatch) -> Result<Self, ArrowError> {
        let expected = SaleSchema::schema();
        if batch.schema().fields() != expected.fields() {
            return Err(ArrowError::SchemaError(format!(
                "expected Sale schema, got {:?}",
                batch.schema()
            )));
        }
        Ok(Self { batch })
    }

    /// Access the sale_id column as a Int64Array.
    pub fn sale_id(&self) -> &Int64Array {
        self.batch.column(0).as_primitive()
    }

    /// Access the customer_name column as a StringArray.
    pub fn customer_name(&self) -> &StringArray {
        self.batch.column(1).as_string()
    }

    /// Access the sale_date column as a Date32Array.
    pub fn sale_date(&self) -> &Date32Array {
        self.batch.column(2).as_primitive()
    }

    /// Access the items column as a ListArray.
    pub fn items(&self) -> &ListArray {
        self.batch.column(3).as_list()
    }

    /// Access the shipping_address column as a StructArray.
    pub fn shipping_address(&self) -> &StructArray {
        self.batch.column(4).as_struct()
    }

    /// Access the total_amount column as a Decimal128Array.
    pub fn total_amount(&self) -> &Decimal128Array {
        self.batch.column(5).as_primitive()
    }

    /// Access the notes column as a StringArray.
    pub fn notes(&self) -> &StringArray {
        self.batch.column(6).as_string()
    }

    /// Number of rows in the batch.
    pub fn num_rows(&self) -> usize {
        self.batch.num_rows()
    }

    /// Access the underlying RecordBatch.
    pub fn batch(&self) -> &RecordBatch {
        self.batch
    }
}
